package com.huypham.trademe;

public class Test {
    /* exchange table
     * 1 epic = 5 ticket
     * 1 rare = 2 ticket
     * 1 uncommon = 1 ticket
     * 16 common = 1 ticket
     * P/s priority taken, common -> epic
     */

    // limit tickets is 64
    // ratioTable like this -> ratioTable[0][0] = 16 (representation for common) ; ratioTable[0][0] = 1 (representation for ticket)
    //...


    public static void main(String[] args) {
        Test test = new Test();
    }
}
